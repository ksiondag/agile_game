# For loop
for( var i=init; check_something(i); i=modified_i ) { }

# TODO: iterative for-loop

# String manipulation
string.match(regularexpression)
    Returns
        match index and matching string
        null if no match

# TODO: document on load
# TODO: get element by id
